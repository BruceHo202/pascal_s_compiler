program id(id, id);
    const id=letter;
    array[num..num,letter..letter] of Boolean id = char;
    var id, id: integer ;function id (var id,id:integer):real;
    begin 
    end;
    begin 
        if id.id then 
            case id relop id of num:;id:while id 
                do repeat id assignop id mulop id until not letter end 
        else ;
        id(id, id);
        id(id(id,id)) 
    end.